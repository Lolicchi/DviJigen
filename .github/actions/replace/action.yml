name: Replace text
description: Replace text
inputs:
  file:
    description: File path
    required: true
  find:
    description: Text to find
    required: true
  replace:
    description: Text to replace
    required: true

runs:
  using: composite
  steps:
  - run: |
      ${{ inputs.find }}
      Set-Content ${{ inputs.file }} (Get-Content ${{ inputs.file }} -raw).indexOf(${{ inputs.find }})
      (Get-Content ${{ inputs.file }} -Raw)
    shell: pwsh
